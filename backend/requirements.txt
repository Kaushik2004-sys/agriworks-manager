# M16.8: pinned to the currently installed, fully tested working versions
# (166/166 backend tests pass with exactly these). Pinning keeps hosting
# builds reproducible instead of drifting with future releases.
Django==6.1.1
djangorestframework==3.18.1
django-cors-headers==4.9.0
mysqlclient==2.2.8
python-dotenv==1.2.3
gunicorn==26.2.0
whitenoise==6.12.0